import React from 'react';
import ReactDOM from 'react-dom';
import ImageResolutionIncreaseApp from './App';

ReactDOM.render(
  <React.StrictMode>
    <ImageResolutionIncreaseApp />
  </React.StrictMode>,
  document.getElementById('root')
);
